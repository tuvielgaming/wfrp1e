As you build the system, we'll add declarations when needed:

src/types/
├── foundry.d.ts       # globals
├── documents.d.ts     # Actor, Item, Journal, etc.
├── applications.d.ts  # UI windows/forms
└── canvas.d.ts        # if you use scenes/maps